package partials

templ PageBanner(title, description string) {
	<div class="mb-4">
		<h1 class="text-2xl font-semibold">{ title }</h1>
		<div class="mt-2">
			<p>{ description }</p>
		</div>
	</div>
}
